#include "Test.h"

void Test::addVec_cpu(float *a, float *b, float *c, int n) {
              for(int i=0;i<n;++i)
              {
        for(int j=0;j<10000000;++j)
                c[i] = sqrt(a[i] * b[i]);
                }
}


